#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mojs/bin/mojs" "$@"
  ret=$?
else 
  node  "$basedir/../mojs/bin/mojs" "$@"
  ret=$?
fi
exit $ret
